{{- $fullName := include "atlas.fullname" . -}}
1. Atlas is being deployed!

2. Get the application URL:
{{- if .Values.atlas.ingress.enabled }}
{{- range $host := .Values.atlas.ingress.hosts }}
  http{{ if $.Values.atlas.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}
{{- else if contains "NodePort" .Values.atlas.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ $fullName }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.atlas.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        You can watch the status of by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ $fullName }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ $fullName }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP:{{ .Values.atlas.service.port }}
{{- else if contains "ClusterIP" .Values.atlas.service.type }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ $fullName }} 8000:{{ .Values.atlas.service.port }}
  echo "Visit http://127.0.0.1:8000"
{{- end }}

3. Check deployment status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "atlas.name" . }}

{{- if .Values.atlas.hostNetwork }}

4. NOTE: hostNetwork is enabled for VPN access.
   The pod will use the host's network stack and VPN tunnel.
{{- end }}

{{- if .Values.vpnSidecar.enabled }}

4. NOTE: VPN Sidecar is enabled.
   Make sure the secret '{{ .Values.vpnSidecar.configSecret }}' contains your VPN configuration.
{{- end }}

{{- if .Values.slackBot.enabled }}

5. Slack Bot is enabled and will connect via Socket Mode.
{{- end }}

{{- if .Values.telegramBot.enabled }}

6. Telegram Bot is enabled and will poll for updates.
{{- end }}
