{{- if .Values.atlas.ingress.enabled -}}
{{- $fullName := include "atlas.fullname" . -}}
{{- $svcPort := .Values.atlas.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "atlas.labels" . | nindent 4 }}
  {{- with .Values.atlas.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.atlas.ingress.className }}
  ingressClassName: {{ .Values.atlas.ingress.className }}
  {{- end }}
  {{- if .Values.atlas.ingress.tls }}
  tls:
    {{- range .Values.atlas.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.atlas.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
