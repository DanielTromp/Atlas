# Atlas MCP Server Configuration

# Atlas API Base URL
ATLAS_API_URL=http://127.0.0.1:8000
# For production (HTTPS):
# ATLAS_API_URL=https://atlas.example.com

# Authentication - use EITHER username/password OR API token
#
# Option 1: Session-based authentication (RECOMMENDED - works with all endpoints)
ATLAS_USERNAME=admin
ATLAS_PASSWORD=your_password_here
#
# Option 2: Bearer token authentication (limited - some endpoints require user context)
# ATLAS_API_TOKEN=your_api_token_here

# SSL Verification
# Set to false only for local development with self-signed certificates
ATLAS_VERIFY_SSL=true
