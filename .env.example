# Copy to .env and fill in values

# NetBox
NETBOX_URL=https://netbox.example.com
NETBOX_TOKEN=your_api_token_here
# Optional: extra debug logging for contact assignments
NETBOX_DEBUG=0

# Logging (general)
LOG_LEVEL=INFO

# Confluence publishing
# Page ID that receives the CMDB Excel attachment
CONFLUENCE_CMDB_PAGE_ID=981533033
# Optional: override page for the devices table (defaults to CONFLUENCE_CMDB_PAGE_ID)
CONFLUENCE_DEVICES_PAGE_ID=981533060
# Optional: override page for the VMs table (defaults to CONFLUENCE_PAGE_ID)
CONFLUENCE_VMS_PAGE_ID=981794840
# Optional: enable macros (requires Table Filter & Charts for Confluence)
# CONFLUENCE_ENABLE_TABLE_FILTER=1
# CONFLUENCE_ENABLE_TABLE_SORT=1

# Atlassian Cloud (Jira & Confluence)
# Base URL of your Atlassian Cloud instance, user email and API token
# Generate token: https://id.atlassian.com/manage-profile/security/api-tokens
ATLASSIAN_BASE_URL=https://your-domain.atlassian.net
ATLASSIAN_EMAIL=your_email@company.com
ATLASSIAN_API_TOKEN=your_api_token_here

# HTTPS (optional)
# When set, the API server runs with TLS if both are provided
# ENREACH_SSL_CERTFILE=certs/localhost.pem
# ENREACH_SSL_KEYFILE=certs/localhost-key.pem
# If your key is encrypted, set password below
# ENREACH_SSL_KEY_PASSWORD=

# Authentication (optional)
# API: Protect endpoints with a Bearer token
# ENREACH_API_TOKEN=choose-a-strong-token
# UI: Protect /app with a password (session-based login at /auth/login)
# ENREACH_UI_PASSWORD=choose-a-strong-password
# Session secret for UI cookies; if omitted a random secret is generated per start
# ENREACH_UI_SECRET=change-me-long-random-string
